@model IEnumerable<BBMIS.Models.Derivation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarBuild.Mnemonic_Bodystyle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fuel.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Motor.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarBuild.Mnemonic_Bodystyle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fuel.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Motor.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DerivationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DerivationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DerivationID })
        </td>
    </tr>
}

</table>
